<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clb.mapper.ReaderMapper">
    <!--注册-->
    <insert id="register">
        INSERT INTO reader(username, password, nickname, gender, age, tel)
        VALUES (#{username}, #{password}, #{nickname}, #{gender}, #{age}, #{tel})
    </insert>
</mapper>